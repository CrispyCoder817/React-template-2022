let VersionData = JSON.Stringify("react-dom");

function Version(VersionData) {
    return(
        <div>
            <p>
                (this.VersionData);
            </p>
                
        </div>
    )
}

export default  Version();